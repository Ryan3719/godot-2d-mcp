# Godot 2D MCP Server

Python MCP transport and session bridge for the Godot 2D MCP editor plugin.

Development commands:

```bash
uv sync --dev
uv run pytest
uv run ruff check .
uv run godot-2d-mcp --transport stdio
```
